name: Build and Deploy to HF Space

on:
  push:
    branches: [ main ]
  workflow_dispatch:

env:
  IMAGE_NAME: registry.hf.space/munzali/rice_diseases_classification:latest
  SPACE_NAME: Munzali/RICE_DISEASES_CLASSIFICATION

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true
      
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      
      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
          pip install -r requirements.txt
          pip install pytest
      
      - name: Run tests
        run: pytest tests/ -v || echo "No tests found"

  build-and-push:
    needs: test
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Hugging Face Registry
        uses: docker/login-action@v3
        with:
          registry: registry.hf.space
          username: munzali
          password: ${{ secrets.HF_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            registry.hf.space/munzali/rice_diseases_classification:latest
            registry.hf.space/munzali/rice_diseases_classification:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Verify image was pushed
        run: |
          echo "✅ Docker image pushed successfully!"
          echo "📦 Image: registry.hf.space/munzali/rice_diseases_classification:latest"

  restart-space:
    needs: build-and-push
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    
    steps:
      - name: Restart Hugging Face Space
        run: |
          curl -X POST "https://huggingface.co/api/spaces/${{ env.SPACE_NAME }}/restart" \
            -H "Authorization: Bearer ${{ secrets.HF_TOKEN }}"
          
          echo "✅ Space restart triggered!"
          
          # Wait and check status
          sleep 30
          
          STATUS=$(curl -s "https://huggingface.co/api/spaces/${{ env.SPACE_NAME }}" \
            -H "Authorization: Bearer ${{ secrets.HF_TOKEN }}" | jq -r '.runtime.stage' 2>/dev/null || echo "unknown")
          
          echo "📊 Space status: $STATUS"
          echo "🔗 https://huggingface.co/spaces/${{ env.SPACE_NAME }}"
